#include <stdio.h>

int main()
{
    int h, b;
    printf("Enter the value of a\n");
    scanf("%d", &h);

    printf("Enter the value of b\n");
    scanf("%d", &b);

    printf("The sum of a and b is %d", h + b);
    return 0;
}