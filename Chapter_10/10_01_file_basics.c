#include <stdio.h>

int main(){
    
FILE *ptr;

   ptr = fopen("sample.txt","r"); // FOR READING THE FILE... // fopen(); used to open the file for reading or writing...
// ptr = fopen("sample2.txt","w");    // FOR WRINTING THE FILE...

    return 0;
}