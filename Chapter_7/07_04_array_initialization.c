#include <stdio.h>

int main()
{
    float a[] = {23, 35, 45};

    printf("The value of a[0] is %f\n", a[0]);

    printf("The value of a[1] is %f\n", a[1]);

    printf("The value of a[2] is %f\n", a[2]);

    return 0;
}